const Contact = () => {

    const template = `
    <section id="contact" class="container">
        <div class="card shadow">
        <h2 class="text-center">Ein Wort an Yeets!</h2>
        <small>Aller Anfang ist schwer, das wissen wir, aber jedes Projekt hat irgendwo seinen Anfang – warum also nicht hier? </small>
           
        </div>
    </section>
  `;

    return template;
};

export default Contact;